{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "codegen:tpg": "type-predicates-generator -a -f 'types/*.ts' --strict-array-check",
    "watch:tpg": "yarn codegen:tpg -w",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "type-predicates-generator": "../"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.11.7",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "tsd": "^0.18.0",
    "typescript": "^4.4.4"
  }
}
